# Backend Configuration
# For local development with local PostgreSQL:
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/christmas_gifts?sslmode=disable
#
# For Neon (managed PostgreSQL):
# DATABASE_URL=postgresql://user:password@neon-host/dbname?sslmode=require
DATABASE_URL=postgres://postgres:postgres@localhost:5432/christmas_gifts?sslmode=disable
PORT=8080

# Frontend Configuration
# Frontend uses Vite proxy configuration and Netlify redirects
# No environment variables needed at build time
# In production (Netlify), the netlify.toml redirect proxies /api/* to the Render backend

